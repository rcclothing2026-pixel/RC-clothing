; Per-directory PHP overrides for the web root (read by PHP-FPM/CGI).
; Ships with every deploy so upload limits are consistent on any host —
; without this the default 2M cap 413s big hero/banner image uploads.
upload_max_filesize = 16M
post_max_size = 16M
max_execution_time = 120
memory_limit = 256M
