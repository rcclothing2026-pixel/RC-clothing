Surface container in four tones.

```jsx
<Card>Default white surface</Card>
<Card variant="inverse">Navy panel</Card>
<Card variant="court" interactive>Green, lifts on hover</Card>
```
Variants: `default` `sunken` `inverse` (navy) `court` (green). `padding`: `sm` `md` `lg`. `interactive` lifts on hover.
