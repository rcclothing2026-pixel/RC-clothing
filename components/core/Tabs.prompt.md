Underline tab bar with a clay active indicator.

```jsx
<Tabs items={[{id:'book',label:'Book'},{id:'events',label:'Events'}]} defaultValue="book" onChange={setTab} />
```
Controlled (`value`) or uncontrolled (`defaultValue`).
