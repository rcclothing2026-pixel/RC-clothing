Square icon-only button. Pass a Lucide (or any) icon SVG as children; always give a `label`.

```jsx
<IconButton label="Menu" variant="solid"><MenuIcon/></IconButton>
```
Variants: `ghost` (default), `solid`, `outline`. Sizes: `sm` `md` `lg`.
